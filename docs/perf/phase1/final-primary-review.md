# Phase 1 Final Primary Audit

**Reviewer lane:** internal GPT-5.6-Terra/high primary  
**Audited HEAD:** `1e97864a3a1bf0a7da9cf46ac91edb775d4d1d3a`  
**Implementation revision:** `19edfbd424cb7d633e597b9beb66401fdcd90364`  
**Scope:** `docs/plans/phase1-plan.md`, D2/D4 in
`docs/tasks/2.fp4_decode_speedup.md`, and the approved FP4-Q residual
deferral. No tests or benchmarks were run by this audit.

## Result

**BLOCKED — AC-6 is not fully satisfied.**

The implementation and recorded primary evidence resolve the prior technical
blocker, but the required *different-model cross-review PASS* has not been
recorded for `19edfbd` (or HEAD `1e97864`). The only committed alternate-model
review is `docs/perf/phase1/cross-review.md`; it reviews `4c31c11` and ends
`NEEDS_FIX`. It cannot serve as a passing review of the post-fix revision.

Because Phase 1 is explicitly stop-on-failure, this missing close criterion is
the exact remaining blocker. Do not mark the phase complete or begin a later
phase until an independent internal-model review of `19edfbd`/`1e97864`
records a pass.

## Primary Findings

| Requirement | Result | Audit basis |
|---|---|---|
| D2 single public entry and mutually exclusive contracts | PASS | `interface.fp4_decode` remains the single public entry. `_kernel.fp4_decode_impl` accepts BF16 `query` or paired keyword-only `query_fp4`/`query_scales` and rejects neither, partial, and ambiguous contracts. |
| Shared core/API | PASS | Both branches converge on one `decode_fp4(...)` call. No second attention/decode core or K/V re-quantization was introduced. |
| Native packed GQA scale layout | PASS | `19edfbd` changes `quantize_query` and `quantize_decode_q_to_padded_fp4` to allocate/write `[32, 4, 1, 4, rest_k, heads_kv, rows]` directly. The quantizer maps each query head into the PackGQA M coordinates and KV-head axis; `decode_fp4` now validates and consumes that native layout directly. |
| No FP4-Q host repack, scale scratch, or mutable repack cache | PASS | `19edfbd` deletes `_pack_sfq_for_gqa`, `_sfq_pack_cache`, `torch.take`, and `index_copy_` from `_decode.py`. The direct FP4-Q branch passes `query_scales` unchanged to the compiled core. |
| No FP4-Q quantization or padded BF16 Q scratch | PASS | The pre-quantized branch does not call `quantize_query`, sets `query_padded_bf16=None`, and rejects FP4-Q with `query_row_indices`. BF16-Q retains its existing conditional padded residual-Q preparation only for the BF16 residual path. |
| Approved residual deferral | PASS | FP4-Q plus BF16 residual is rejected before launch because a pre-quantized call has no `query_padded_bf16`; BF16-Q residual support remains intact. This is the user-approved Phase 1 boundary, not a regression. |
| Exact numerical gate | PASS by committed transcript | The parameterized FP4-Q test covers MHA `(8,8)`, GQA `(32,8)`, and MQA `(32,1)` at page-aligned length 256, obtains FP4-Q tensors from the repository quantizer, and asserts `torch.equal` against BF16-Q output. |
| Negative validation | PASS in implementation; focused regressions present | Runtime validation rejects bad packed dtype/shape/contiguity, scale shape/layout, device mismatches, row/head incompatibility, and invalid contract combinations before launch. The committed tests exercise neither/partial/ambiguous contracts, indexed FP4-Q rejection, wrong packed dtype/shape, wrong scale shape/layout, and incompatible head metadata. |
| D4 benchmark separation and labels | PASS | Benchmark variants retain `fp4_pure_bf16q` separately from `fp4_pure_fp4q`; FP4-Q is labeled `fp4_q_fp4_kv`, BF16-Q is `bf16_q_fp4_kv`, and ratios select the lower-latency FA4 split=1/heuristic baseline. |
| Clean provenance and transcript | PASS | `short-case-final.json` records `commit=19edfbd...`, `dirty=false`, 10 warmups and 50 measured CUDA-event iterations. `test-results.txt` records `56 passed` in 73.64 s. `1e97864` adds only this clean verification evidence and report update, preserving the measured implementation revision. |
| Required independent cross-review | **BLOCKER** | The extant cross-review is a pre-fix `NEEDS_FIX` review of `4c31c11`; no committed passing alternate-model review covers the final native-layout implementation. |

## Evidence Notes

- The original primary finding—per-call GQA scale repacking into shared mutable
  scratch—is resolved in source, not merely hidden by a cache. The final path
  has no host-side scale transform between `quantize_query(..., heads_kv=...)`
  and `decode_fp4(...)`.
- The final JSON is correctly attributable to `19edfbd`, rather than falsely
  claiming it was generated by the later evidence-only commit `1e97864`.
- The existing `cross-review.md` remains useful historical evidence: its
  benchmark-provenance, label, scratch/cache, and negative-validation findings
  motivated the recorded follow-up. Its verdict remains non-passing and is not
  superseded by an independent review artifact.

## Required Close Action

Record a new different-internal-model cross-review against
`19edfbd`/`1e97864`, explicitly confirming the native GQA scale write,
removal of host repack/cache, D2/D4 behavior, residual deferral, final JSON,
and 56-pass transcript. If that review passes without a new finding, this
primary audit may be updated to `COMPLETE`.

**Terminal status: BLOCKED — missing post-fix alternate-model cross-review PASS.**
